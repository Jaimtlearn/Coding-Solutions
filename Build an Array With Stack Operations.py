class Solution
    def buildArray(self, target List[int], n int) - List[str]
        ans = []
        # temp = []
        for i in range(1,target[-1]+1)
            # if temp == target
            #     return ans
            if i in target
                ans.append(Push)
                # temp.append(i)
            else
                ans.append(Push)
                ans.append(Pop)
        return ans